import 'package:flutter/material.dart';

class AppColors {
  // 🎨 الألوان الأساسية (بنفس الأسماء القديمة لكن من الباليتة الجديدة)
  static const Color primary = Color(0xFF1A1A1A); // أسود غامق
  static const Color secondary = Color(0xFF333333); // رمادي غامق
  static const Color bg = Color(
    0xFFFFFFFF,
  ); // أبيض Color(0xFFE6E6E6); // رمادي فاتح
  static const Color textPrimary = Color(0xFF4D4D4D); // رمادي متوسط
  static const Color textSecondary = Color(0xFF666666); // رمادي أفتح
  static const Color cardBg = Color(0xFFE6E6E6); // رمادي فاتح

  // 🎨 باقي ألوان الباليتة الجديدة
  static const Color greyDark = Color(0xFF808080);
  static const Color greyMedium = Color(0xFF999999);
  static const Color greyLight = Color(0xFFB3B3B3);
  static const Color greyLighter = Color(0xFFCCCCCC);

  static const Color green = Color(0xFF0C9409); // أخضر
  static const Color red = Color(0xFFED1010); // أحمر
}
